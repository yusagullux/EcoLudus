const app = { provider: "nextjs" };
const auth = { provider: "session-cookie" };
const db = { provider: "postgres-compat" };
const storage = null;
const analytics = null;

export { app, auth, db, storage, analytics };
